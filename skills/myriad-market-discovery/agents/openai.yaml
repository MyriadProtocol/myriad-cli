interface:
  display_name: "Myriad Market Discovery"
  short_description: "Discover and filter `markets` results"
  default_prompt: "Use $myriad-market-discovery for `myriad markets ...` discovery and route `myriad ob ...` requests to $myriad-orderbook."
